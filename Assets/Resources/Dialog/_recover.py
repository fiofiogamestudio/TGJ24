import shutil

source_file = "dialog.json"
backup_file = "dialog_backup.json"

shutil.copy(backup_file, source_file)

source_file = "dialog.json"
backup_file = "dialog_backup.json"

shutil.copy(backup_file, source_file)